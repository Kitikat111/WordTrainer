<UserControl x:Class="WordTrainer.Views.TestControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="500">
    <Grid Margin="20,20,10,20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="749*"/>
            <ColumnDefinition Width="11*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" MinHeight="43"/>
            <RowDefinition Height="Auto" MinHeight="62"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto" MinHeight="245.28"/>
        </Grid.RowDefinitions>

        <ComboBox ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}" Margin="0,0,274,0" VerticalAlignment="Center" FontSize="16" Padding="10,5" Height="30" FontFamily="RomanT"/>

        <TextBlock Text="{Binding CurrentWord.Original}" FontSize="48" Grid.Row="1" VerticalAlignment="Center" Height="55" Margin="73,0,56,0" TextAlignment="Center" FontFamily="News701 BT" FontWeight="Bold" Foreground="Black"/>

        <ListBox ItemsSource="{Binding TranslationOptions}" SelectedItem="{Binding SelectedTranslation}" Margin="0,0,0,132" Grid.Row="2" FontSize="18" Padding="10,5" Grid.RowSpan="3" Grid.ColumnSpan="2" d:ItemsSource="{d:SampleData ItemCount=5}" FontFamily="Segoe UI Black"/>

        <Button Command="{Binding CheckAnswerCommand}" Content="Проверить" Grid.Row="4" FontSize="18" Padding="10,5" Height="37" Margin="0,113,0,0" HorizontalAlignment="Left" Background="#FF3FB85A" Width="189" VerticalAlignment="Top" FontFamily="Segoe UI Semibold" FontWeight="Bold"/>

        <StackPanel Orientation="Horizontal" Grid.Row="4" Margin="12,155,2,69" Grid.ColumnSpan="2">
            <TextBlock Text="Правильно:" FontSize="16" Width="123" FontFamily="RomanT"/>
            <TextBlock Text="{Binding Statistics.CorrectAnswers}" FontSize="16" Margin="5,0,0,0"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="4" Margin="12,182,0,41" Grid.ColumnSpan="2">
            <TextBlock Text="Неправильно:" FontSize="16" Width="129" FontFamily="RomanT"/>
            <TextBlock Text="{Binding Statistics.IncorrectAnswers}" FontSize="16" Margin="5,0,0,0"/>
        </StackPanel>

        <TextBlock Text="{Binding CorrectAnswer, FallbackValue=''}" FontSize="16" FontWeight="Bold" Foreground="#FF451010" Margin="10,210,143,10" Grid.Row="4"/>
    </Grid>
</UserControl>
